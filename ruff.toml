target-version = "py310"

[lint]
# Start with correctness rules that the legacy module can pass without a
# formatting rewrite. Expand this set as code moves into focused modules.
select = ["E4", "E7", "E9", "F"]
